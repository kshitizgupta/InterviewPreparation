package arrays;

class SubArray {
        int startIndex;
        int endIndex;
        int sum;

        public String toString(){
            return String.format("startIndex = %d endIndex = %d sum = %d", startIndex, endIndex,sum);
        }
    }